export class User {

    constructor() {

    }

    async getUserById(idUser: string) {
        /*
        const respons = await fetch(``, {
        })

        if(respons.ok) {
            return (await respons.json)
        }
        */
    }
}